pipeline 
{
    agent any
    
    stages
    {
        stage("Git Clone") 
        {
            steps {
                git 'https://github.com/ChethanGowda6464/aws_devops_projects.git'
                  }
         }
            stage("Maven Builds")
            {
            steps {              
                sh "mvn validate"
                sh "mvn clean"
                sh "mvn compile"
                sh "test -compile"
                sh "mvn test"
                sh "mvn install"
                  }
            }
    }
}
